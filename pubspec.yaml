name: synccal
description: "A new Flutter project."
publish_to: 'none'

version: 0.0.03

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  go_router: ^9.0.0
  intl: ^0.20.2
  path_provider: ^2.1.0
  provider: ^6.1.2

  # Hive - Best local database for your use case
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_contacts: ^1.1.9+2
  shared_preferences: ^2.3.0
  uuid: ^4.4.0
  http: ^1.2.0
  package_info_plus: ^10.1.0
  url_launcher: ^6.3.2
  pub_semver: ^2.2.0
  connectivity_plus: ^7.1.1
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  hive_generator: ^2.0.1
  build_runner: ^2.4.9
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.11.0

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icons/syncal.png"

flutter:
  uses-material-design: true