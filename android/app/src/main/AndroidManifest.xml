<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.SynCal">

    <!-- Core permissions -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <!-- For self‑update -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Foreground service for active batch sending -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Required for remoteMessaging type (Android 14+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING" />

    <!-- SIM selection (dual‑SIM) -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Exact-alarm scheduling for scheduled-message delivery. Declared use
         case: user-set delivery times for scheduled SMS, analogous to a
         reminder/calendar alarm — same category Play Console expects for
         this permission. We still fall back to inexact alarms at runtime
         if this is denied (see AlarmScheduler.kt), so the app degrades
         gracefully rather than depending on it being granted. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- Lets AlarmManager wake the device for due scheduled messages. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="true" />

    <application
        android:label="SynCal"
        android:name=".SynCalApplication"
        android:icon="@mipmap/ic_launcher"
        android:requestLegacyExternalStorage="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Foreground service – only runs during active batch sending -->
        <service
            android:name=".SmsForegroundService"
            android:foregroundServiceType="remoteMessaging"
            android:exported="false" />

        <!-- Wakes the app (via a short-lived headless Flutter engine) when
             an alarm fires to check/send due scheduled messages. Does not
             require MainActivity or the cached UI engine to be alive. -->
        <receiver
            android:name=".ScheduleAlarmReceiver"
            android:exported="false" />

        <!-- Restores the alarm schedule after device reboot (AlarmManager
             entries do not survive a reboot) and checks for anything that
             came due while the device was off. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- FileProvider for APK installation -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

    <!-- Needed for APK install intent -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="application/vnd.android.package-archive" />
        </intent>
    </queries>
</manifest>